<!--index.wxml-->
<view class="container">
  <view class="top_bg">
    <image src="../../assets/bg.png"></image>
  </view>
  <view class="login-btn" bindtap="toReg">{{
    storeView.isLogin ? "hello" : "Login"
  }}</view>
</view>
